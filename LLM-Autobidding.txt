这是一个非常棒的工程规划。将需求清晰地整理成结构化的 Prompt 交给 Claude Code（或其他代码智能体），能够极大提高它生成正确代码的效率。以下是我为你整理的专门写给 Claude Code 的需求文档。你可以直接全选复制并发送给它：Project: AuctionNet Synthetic Data Generation for LLM-Bidding DPO1. 任务背景与目的 (Purpose)我们正在研发一个基于大语言模型（LLM）的自动出价智能体（LLM-Bidding Agent）。由于线上的真实出价日志经过脱敏，丢失了宏观环境特征（如时间、节假日、大盘流量变化等），我们计划使用开源的 AuctionNet 竞价模拟器作为一个“数字孪生沙盒”。核心目的：编写 Python 脚本，在 AuctionNet 中主动构造不同“宏观语义场景”下的模拟环境。针对每个场景，投放不同策略的基准出价系数（$\lambda$）作为探针，收集微观的竞价反馈（总花费、转化数、真实 CPA）。最后，将这些反馈转化为大模型微调所需的 DPO（直接偏好优化）文本数据对，使得 LLM 能够学习到“宏观语义 -> 预算约束 -> 最优出价系数 $\lambda$”的内在映射逻辑，并拒绝会引发分布外风险（OOD/爆 CPA）的错误决策。2. 核心工作流与要求 (Workflow & Requirements)请按照以下四个步骤编写 Python 数据流水线：Step 1: 构建宏观场景到微观分布的映射器 (Scenario Configurator)需要编写一个配置生成函数，接收一段宏观环境描述，并据此修改 AuctionNet 模拟器的环境生成参数。参数映射示例：场景 A ("今天是星期日晚上十一点，属于深夜冲动消费期")：整体流量（Impression/PV）降低 30%，但转化率 pValue 的方差（pValueSigma）增大，竞争底价 leastWinningCost 降低 15%。场景 B ("双十一最后两小时，预算充足，全盘抢量")：整体流量（Impression/PV）增加 300%，leastWinningCost 指数级增加。要求： 在代码中预设至少 3-5 种典型场景的配置模版。Step 2: 探针策略并行模拟 (Multi-Probe Simulation)在上述生成好的特定 AuctionNet 环境中，针对相同的初始状态（预算 Budget、目标约束 CPA Constraint），并行或顺序跑多组不同的固定出价系数 $\lambda$（例如设置一组 Candidate Lambda: [0.5, 0.8, 1.0, 1.2, 1.5, 2.0]）。竞价逻辑： 每个 PV 的实际出价设定为 $b_i = \lambda \cdot pValue_i$。数据采集： 记录每种 $\lambda$ 跑完该周期的实际消耗总花费 (realAllCost) 和总转化数 (realAllConversion)。Step 3: 模拟结果评分与偏好提取 (Evaluation & Preference Extraction)模拟完成后，根据以下规则对不同 $\lambda$ 的表现进行评估和排序：计算真实 CPA： realCPA = realAllCost / realAllConversion。安全过滤： 优先过滤出 realCPA <= CPA Constraint 的策略集合（符合约束的动作）。最优（Chosen）： 在符合约束的集合中，选取带来最大总转化数的 $\lambda$。最劣（Rejected）： 选取导致 realCPA 严重超过 CPA Constraint 的 $\lambda$（激进导致 OOD），或者转化数极低且花费极少的 $\lambda$（过度保守）。Step 4: 组装 DPO 数据格式 (Data Formatting)编写一个模版函数，将 Step 3 提取的最优和最劣系数，结合当前的环境状态，格式化为最终的 JSON/JSONL 格式。3. 输出数据格式规范 (Output Format Specification)最终生成的 .jsonl 文件中的每一行必须严格遵循以下 JSON Schema。脚本需要自动拼接其中的【推理过程】，用以模拟 LLM 的思维链（CoT）：JSON{
  "instruction": "作为高级出价智能体，请根据当前环境和约束，输出最优的出价系数 lambda 并给出推理逻辑。",
  "context": "【宏观环境】今天是星期日晚上十一点，深夜冲动消费期，流量总盘下降，底价降低。\n【微观约束】当前总预算 500.0，CPA 目标约束为 8.0。历史状态向量：[...]",
  "chosen": "【推理过程】当前 CPA 健康且预算有结余。考虑到夜间流量基数变小，但竞争对手休息导致大盘底价降低，这是一个以较低成本捕获高转化冲动流量的机会。为了最大化拿量且不突破 CPA 8.0 的限制，我们应该抓住机会适当放宽出价边界。\n【决策出价】建议设定系数 lambda = 1.2。",
  "rejected": "【推理过程】考虑到今天是星期日晚上，流量非常稀少，我们要采取极度激进的策略把钱花出去，强行抢量。\n【决策出价】建议设定系数 lambda = 2.0。"
}
执行要求总结：请基于上述需求，为我编写一套完整的 Python pipeline，包含场景定义、AuctionNet 模拟调用、策略评估以及 DPO 数据写入逻辑。代码需具备良好的模块化和注释说明。